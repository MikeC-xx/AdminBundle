{% set obj = entity %}
{% set property = column.property is defined ? column.property : column %}
{% for key in property|split('.') %}
{% set obj = attribute(obj, key) %}
{% endfor %}
{{ obj|propertyValue(column) }}
