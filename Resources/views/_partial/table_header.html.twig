{% for column in columns %}
    <th{{ column.attr is defined ? column.attr|htmlAttributes }}>{{ column.property is defined ? column.property|humanize|trans : column|humanize|trans }}</th>
{% endfor %}
<th></th>
